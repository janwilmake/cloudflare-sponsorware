import { Env } from "./github-login";

export const getLifetimeValue = (request: Request, env: Env) => {
  return {};
};

export const middleware = (request: Request, env: Env) => {
  return undefined;
};
